package com.algorithms.sort;

//对于每个元素x, 确定小于x的元素的个数,利用这一信息,就可以把x直接放在正确的位置 (需要)
public class CountingSort {
     public  static  void main(String[] args){
         System.out.print("aaaaa\n");
     }
}
